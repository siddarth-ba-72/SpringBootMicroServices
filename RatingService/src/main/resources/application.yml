server:
  port: 8083

spring:
  application:
    name: RatingService
  data:
    mongodb:
      uri: mongodb://localhost:27017
      database: microservices
  config:
    import: configserver:http://localhost:8085